# Backend environment example for AI Copilot (FastAPI)
# Copy to .env and adjust as needed for local development.

# CORS: Allow the frontend origin (adjust if your frontend runs elsewhere)
FRONTEND_ORIGIN=http://localhost:3000

# Optional: Enable Supabase integration (default is disabled)
# Set to "true" to enable. When enabled, SUPABASE_URL and at least one key must be set.
ENABLE_SUPABASE=false

# Optional: Supabase configuration
# Base URL of your Supabase project (e.g., https://xyzcompany.supabase.co)
SUPABASE_URL=

# One of the following keys must be present if ENABLE_SUPABASE=true
# Service role key (server-side, highly privileged; do NOT expose to frontend)
SUPABASE_SERVICE_ROLE_KEY=
# Anonymous key (client-side safe; limited privileges)
SUPABASE_ANON_KEY=

# Optional: JWT secret for Supabase-authenticated flows (if applicable)
SUPABASE_JWT_SECRET=

# Optional: OpenAI (server-side only)
# If set, backend will use OpenAI Chat Completions for replies; otherwise it falls back to a deterministic response.
# NEVER expose this key to the frontend or commit it to source control.
OPENAI_API_KEY=
# Optional model; if empty, backend uses a safe default (gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini

# Optional: OpenAI (server-side only)
# If set, backend will use OpenAI Chat Completions for replies; otherwise it falls back to a deterministic response.
# NEVER expose this key to the frontend or commit it to source control.
OPENAI_API_KEY=
# Optional model; if empty, backend uses a safe default (gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini
